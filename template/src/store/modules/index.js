
import * as types from '../mutation-type'

// init state
const state = {
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  actions,
  getters,
  mutations
}

